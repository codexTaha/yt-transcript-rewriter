# ─── Supabase ────────────────────────────────────────────────────────────────
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# ─── YouTube Data API v3 ─────────────────────────────────────────────────────
# Required for playlist/channel/video discovery.
# Get yours at: https://console.cloud.google.com/ → Enable "YouTube Data API v3"
YOUTUBE_API_KEY=your_youtube_data_api_v3_key

# ─── Python / Transcript Auth ────────────────────────────────────────────────
# PYTHON_BIN: Explicit path to the Python executable that has youtube-transcript-api installed.
# If not set, the app auto-detects: project venv → system python3.
# For local dev, point this to your venv:
#   PYTHON_BIN=/home/taha/YT-Tools/yt-transcript-rewriter/venv/bin/python3
#   PYTHON_BIN=/home/taha/venv/bin/python3
PYTHON_BIN=

# YOUTUBE_COOKIES_FILE: Path to a Netscape-format cookies.txt exported from your browser.
# This is the most reliable way to fetch transcripts locally without a proxy.
# Export via browser extension: "Get cookies.txt LOCALLY" (Chrome/Firefox)
#   YOUTUBE_COOKIES_FILE=/home/taha/cookies.txt
YOUTUBE_COOKIES_FILE=

# Optional proxy auth (Webshare rotating residential — paid tier required)
WEBSHARE_PROXY_USERNAME=
WEBSHARE_PROXY_PASSWORD=

# Optional generic proxy (host:port, no protocol prefix)
PROXY_URL=

# ─── App URL ─────────────────────────────────────────────────────────────────
# REQUIRED for worker pump self-calls. Without this, the extract pump fires
# fetch() calls to an undefined URL and videos never get processed.
# Local dev: http://localhost:3000
# Production: your Vercel URL e.g. https://yt-transcript-rewriter.vercel.app
NEXT_PUBLIC_APP_URL=http://localhost:3000
